package com.example.madeinburundi.ui.theme

import androidx.compose.ui.graphics.Color


val GreenMIB = Color(0xFF0F9012)
// --- Light Theme Colors ---
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFFE91E63)
val LightBackground = Color(0xFFFEFBFF)
val DarkText = Color(0xFF1B1C17)

// =========================================================================
// --- NEW & IMPROVED Colors for Dark Theme ---
// =========================================================================

// A darker, less saturated green for the primary color in dark mode.
// This reduces eye strain compared to the very bright GreenMIB.
val GreenDark = Color(0xFF6DDD6F)

// A rich, warm gold to serve as a beautiful accent color alongside the green.
val GoldAccentDark = Color(0xFFE5C07B)

// A soft, muted cream color that's easier on the eyes than the default purple.
val CreamAccentDark = Color(0xFFD7C4BB)

// A dark charcoal background, which is less harsh than pure black.
val DarkBackground = Color(0xFF1B1C17)

// A light, off-white for text, which is more comfortable to read than pure white.
val LightText = Color(0xFFE4E3DB)